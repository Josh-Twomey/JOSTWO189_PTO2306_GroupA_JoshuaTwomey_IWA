script.js:

- I decided to use Not a Number function to check for numerical values. typeof would not filter through each character but the whole variable. 
const primaryValid = typeof number == primaryPhone -> const primaryValid = isNaN(primaryPhone) ? false : true;